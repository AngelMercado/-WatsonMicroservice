security:
  basic:
    enabled: false
server:  
  port: 5555
  
eureka:
  instance:
    prefer-ip-address: true
  client:
    regiterWithEureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
zuul:
  routes:
    userservice: /user/**
    itemservice: /item/**
  ignored-services: '*'
  prefix: /api